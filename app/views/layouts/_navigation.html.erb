    <div class="ui huge menu inverted">
  <a class="item" href="<%= root_path %>">
    Chatroom
  </a>
  <a class="item">
    Messages
  </a>
  <div class="right menu">
    <div class="ui dropdown item">
      Account <i class="dropdown icon"></i>
      <div class="menu">
        <% if !logged_in? %>
          <a class="item" href="<%= login_path %>">Login</a>
        <% end %>
        <% if logged_in? %>
          <%= link_to "Logout", logout_path, data: {turbo_method: :delete}, class: "item" %>
        <% end %>
      </div>
    </div>
    
        <% if !logged_in? %>
          <div class="item">
            <%= link_to "Log in", login_path, class: "ui primary button" %>
          </div>
        <% else %>
          <div class="item">
            <%= link_to "Log out", logout_path, data: {turbo_method: :delete}, class: "ui orange button" %>
          </div>

        <% end %>
    </div>
  </div>
</div>